Here’s a clear summary table for those `txid*` functions, their purpose, and how to interpret results:

| Function                                                | Purpose                                                                                                                | Typical Use                                                     | Result Interpretation                                                                                |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| **txid\_current()**                                     | Returns the current transaction ID for the current session (always assigned, even if you haven't done any writes yet). | Debugging transaction wraparound, monitoring current XID usage. | A steadily increasing bigint; large gaps possible due to aborted/unused transactions.                |
| **txid\_current\_if\_assigned()**                       | Returns the current transaction ID **only if** one has been assigned (otherwise NULL).                                 | Avoids forcing XID assignment for read-only transactions.       | NULL = no XID assigned yet; otherwise same as `txid_current()`.                                      |
| **txid\_current\_snapshot()**                           | Returns a `txid_snapshot` object containing xmin, xmax, and in-progress transaction IDs visible to this transaction.   | MVCC visibility checks, understanding snapshot contents.        | Used with `txid_snapshot_xmin/xmax/xip` to inspect which transactions are visible.                   |
| **txid\_snapshot\_in(cstring)**                         | Converts a text representation into a `txid_snapshot`.                                                                 | Parsing snapshot strings from logs or external sources.         | Returns a snapshot object usable in other `txid_snapshot_*` functions.                               |
| **txid\_snapshot\_out(txid\_snapshot)**                 | Converts a `txid_snapshot` into a text representation.                                                                 | Exporting snapshot for inspection or sharing.                   | Text format: `xmin:xmax:xip_list`.                                                                   |
| **txid\_snapshot\_recv(internal)**                      | Receives a binary representation of a snapshot (internal use in protocols).                                            | Low-level replication/protocol handling.                        | Rarely used manually; mainly internal to PostgreSQL.                                                 |
| **txid\_snapshot\_send(txid\_snapshot)**                | Sends a snapshot in binary form (internal).                                                                            | Binary protocol snapshot transfer.                              | Internal use only.                                                                                   |
| **txid\_snapshot\_xip(txid\_snapshot)**                 | Returns the list of in-progress transaction IDs for the snapshot.                                                      | Checking which transactions are still active.                   | Each bigint in the set = active transaction ID.                                                      |
| **txid\_snapshot\_xmax(txid\_snapshot)**                | Returns the xmax (upper bound) from a snapshot.                                                                        | Understanding snapshot's visibility horizon.                    | All TXIDs < xmax are either in-progress or committed at snapshot time.                               |
| **txid\_snapshot\_xmin(txid\_snapshot)**                | Returns the xmin (lower bound) from a snapshot.                                                                        | Identifying oldest transaction visible.                         | All TXIDs < xmin are definitely committed and visible.                                               |
| **txid\_status(bigint)**                                | Returns `'in progress'`, `'committed'`, or `'aborted'` for a given TXID.                                               | Post-mortem debugging of specific transaction IDs.              | Useful to check if a stuck XID is holding back vacuum.                                               |
| **txid\_visible\_in\_snapshot(bigint, txid\_snapshot)** | Returns true if a given TXID is visible in the provided snapshot.                                                      | Custom visibility checks, debugging anomalies.                  | `true` = visible to snapshot, `false` = not visible (still in progress or aborted at snapshot time). |

If you want, I can also diagram how `xmin` / `xmax` / `xip` relate to vacuum wraparound prevention and the problem you’re currently seeing. That would make it easier to interpret these results in your stuck-VACUUM situation.

