select pid, usename,now()-xact_start, wait_event, query from pg_stat_activity where state='active' order by 3 desc;

SELECT
  a.pid,
  a.query_id,
  s.query AS full_query
FROM pg_stat_activity a
JOIN pg_stat_statements s ON a.query_id = s.queryid
WHERE a.pid = 108408;


